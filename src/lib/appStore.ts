// The app's App Store id, shared by every page that emits the Safari Smart App
// Banner (`apple-itunes-app` meta tag). Universal links never fire for URLs
// entered directly in Safari's address bar or followed within hire42.com, so
// the banner is the only in-Safari path into the app — see
// docs/features/done/SHARE_LISTING.md.
export const APP_STORE_ID = '6763940838'
